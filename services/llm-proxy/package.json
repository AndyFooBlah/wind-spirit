{
  "name": "@wind-spirit/llm-proxy",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Wind Spirit model proxy: Firebase auth, per-user token quotas, class-based Gemini routing on Vertex AI.",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -b",
    "start": "node dist/server.js",
    "test": "vitest run",
    "invite": "tsx scripts/invite.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "0.125.0",
    "@anthropic-ai/vertex-sdk": "^0.19.8",
    "@google/genai": "^2.22.0",
    "ajv": "^8.20.0",
    "firebase-admin": "^14.4.0",
    "google-auth-library": "^10.9.1"
  },
  "devDependencies": {
    "@types/node": "^26.6.1",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  },
  "engines": {
    "node": ">=24"
  }
}
